Nonlinear finite-element models in mechanics, scalar PDEs, and design
optimization often combine three computational difficulties. The local model may
be easiest to write as an energy or constitutive law whose derivatives are
tedious to assemble by hand. The global problem then requires robust nonlinear
globalization and sparse Newton--Krylov linear algebra. Finally, the
implementation must remain comparable across reference formulations,
derivative approximations, and distributed-memory solver choices. A useful
scientific toolset for this class of problems should therefore expose not
only a modeling interface, but also the derivative route, assembly layout,
nonlinear solver policy, and preconditioner design.

We develop and evaluate such a scientific toolset for nonlinear finite-element
energy solves and optimization. In the primary \jaxpetsc{} realization, JAX
represents local energies and constitutive laws, while PETSc
owns sparse vectors and matrices, nonlinear globalization, Krylov methods, and
preconditioners. The \purejax{} and \fenics{} formulations serve as reference
formulations where they are available; JAX-FEM, FEniCSx-family work, and the
\v{C}erm{\'a}k--Sysala--Valdman MATLAB elastoplastic implementation lineage
provide scoped external context and comparison surfaces for selected
observables, not comprehensive package-level benchmarks. Where we report
external numerical comparisons, they are restricted to explicitly matched
benchmark definitions and observables.

\subsection{Contributions}
\label{sec:intro-contributions}

We contribute a \jaxpetsc{} scientific toolset in which derivative
construction, sparse ownership, nonlinear globalization, and preconditioning are
treated as coupled design choices for nonlinear FEM energy minimization. The
implemented toolset is organized around three solver components:
\begin{enumerate}
  \item \textbf{Globalized energy minimization.} We compare line-search,
        trust-region, and hybrid globalization choices on representative
        nonlinear finite-element benchmarks, and we keep the reported
        conclusions tied to the tested benchmark definitions.
  \item \textbf{Derivative construction in sparse assembly.} We implement and compare
        element automatic differentiation, constitutive automatic
        differentiation, and colored sparse finite-difference recovery inside
        the same sparse assembly path.
  \item \textbf{Distributed PETSc realization.} We connect the nonlinear and
        derivative choices to PETSc Krylov methods and preconditioners,
        including Hypre, GAMG, and problem-specific polynomial multigrid
        policies used in the larger mechanics and topology runs.
\end{enumerate}

This organization lets the numerical experiments vary derivative information and
solver policy without changing the benchmark definition.

We evaluate these components on six benchmark families: $p$-Laplace, scalar
Ginzburg--Landau, hyperelasticity, two- and three-dimensional
Mohr--Coulomb-type plasticity, and topology optimization. The scalar problems
isolate formulation and derivative agreement. Hyperelasticity adds
finite-strain mechanics and a narrow JAX-FEM external comparison. The
plasticity benchmarks test nonsmooth constitutive structure,
reference-observable agreement, and multigrid policy. Topology optimization
tests a distributed design-and-mechanics loop. The geomechanics context follows
the finite-element strength-reduction and 3D slope-stability literature
\citep{tschuchnigg2015nonassociated,sysala2017returnmapping,sysala2021optimization,sysala2025convexoptimization,sysala2025advancedcontinuation},
but the plasticity object studied here is explicitly an algorithmic endpoint
surrogate, not a claim of path-consistent incremental-history equivalence.

\subsection{State of the Art and Positioning}
\label{sec:intro-sota}

For the purposes of this paper, the most relevant comparison axes are the
modeling layer, the scope of differentiation, the form in which second-order
information enters the computation, the parallel execution model, and the benchmark
families closest to our suite. \Cref{tab:sota-comparison} summarizes selected
documented capabilities and roles; \Cref{sec:related} gives the source-specific
discussion.

\begin{table}[H]
  \centering
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \input{tables/generated/sota_framework_comparison.tex}
  \caption{Selected related computational roles for the software and literature
  families most relevant to this paper. The entries record documented roles
  rather than subjective scores.}
  \label{tab:sota-comparison}
\end{table}

The comparison in \Cref{tab:sota-comparison} places the present toolset between
high-level FEM and adjoint ecosystems, JAX-native differentiable mechanics, and
recent JAX--solver bridge architectures. The paper emphasizes a joint treatment
of local differentiation, PETSc-owned sparse MPI assembly, nonlinear
globalization, and preconditioner policy across a heterogeneous benchmark suite.
The detailed literature categories are discussed in \Cref{sec:related}; the
\purejax{}, \fenics{}, JAX-FEM, and the slope-stability reference-line tests
serve here as scoped checks for specific benchmark definitions and observables.

The paper is organized as follows. \Cref{sec:related} deepens the literature
context behind the summary above. \Cref{sec:methodology} describes the common
finite-element energy notation, derivative routes, nonlinear globalization, and
sparse derivative recovery. \Cref{sec:implementation} explains the primary
\jaxpetsc{} implementation and the narrower \purejax{} and \fenics{} reference
implementations used in selected checks.
\Cref{sec:benchmarks} defines the benchmark suite, with each benchmark separated
into literature model and implemented discrete problem. \Cref{sec:validation}
then reports validation and external-comparison evidence, deliberately
separating hyperelastic JAX-FEM agreement from the three-dimensional plasticity
endpoint-surrogate comparison. \Cref{sec:numerical-examples}
reports timing, scaling, derivative-route, and discretization-comparison
evidence.
\Cref{sec:solver-evidence} adds solver-policy diagnostics, followed by a
discussion of limitations.
