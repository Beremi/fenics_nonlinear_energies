\label{sec:validation}

This section separates validation and external-comparison evidence from the
performance results that follow. The distinction is important because the
paper uses different levels of evidence for different claims: Hyperelasticity
has a narrow external companion comparison against JAX-FEM, while
\plasticitythree{} has a source-family validation ladder for the implemented
endpoint surrogate.

\subsection{Comparison Metrics}
\label{sec:val-metrics}

For vector and field comparisons with nonzero reference norm, we report
relative differences of the form
\begin{equation}
  \delta_M(a,b)
  =
  \frac{\|a-b\|_M}
       {\|a\|_M},
  \label{eq:validation-relative-difference}
\end{equation}
where $a$ is the reference observable and $M$ denotes the discrete norm used for
the comparison. For nodal displacement vectors, $M$ is the Euclidean norm on the
compared free degrees of freedom. For sampled profiles and quadrature-derived
fields, $M$ denotes the corresponding sampled or quadrature-weighted norm used
by the comparison. The reported field and profile observables have nonzero
reference norms. Scalar work and energy differences use the same
reference-denominator convention; the hyperelastic energy post-comparison uses
an absolute denominator floor of $10^{-12}$.

The hyperelastic companion comparison uses a 5\% acceptance threshold for the
final energy, full-field displacement, centerline displacement, and
$u_{\max}$-curve differences. The \plasticitythree{} Layer 2 acceptance gate
uses three thresholds from its validation manifest: at most $0.03$ relative
difference in the highest successful load factor, at most $0.05$ relative
$L^2$ difference for the $u_{\max}(\lambda_{\mathrm{sr}})$ curve, and at most $0.10$
relative $L^2$ difference for the endpoint displacement. The deviatoric-strain
and boundary-profile differences in \Cref{tab:plasticity3d-validation} are
diagnostics, not acceptance gates.

\subsection{Hyperelasticity Companion Comparison}
\label{sec:val-hyperelasticity}

The hyperelasticity energy is defined in \Cref{sec:bench-he}. The external
comparison here is deliberately narrow and uses a serial companion case rather
than the 24-step rotating benchmark path. Both implementations use the same
level-1 mesh and the same right-face $x$-translation schedule
$d=(0.0025,0.005,0.0075,0.01)$, then post-compare both terminal states with
the energy from
\cref{eq:hyperelasticity-energy}. This supports a solution-level comparison
for this hyperelastic problem only. It should not be read as a plasticity
validation, a broad software comparison, or constitutive-law identity between
the two implementations: the JAX-FEM companion implementation uses a
logarithmic-$J$ Piola form, whereas \cref{eq:hyperelasticity-energy} uses the
$D_1(J-1)^2$ volumetric penalty.

\begin{figure}[H]
  \centering
  \begin{subfigure}[t]{0.46\PaperFigureFull}
    \centering
    \includegraphics[width=\linewidth]{jax_fem_hyperelastic_baseline_energy_history.pdf}
    \caption{Terminal energy over the matched displacement schedule.}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.46\PaperFigureFull}
    \centering
    \includegraphics[width=\linewidth]{jax_fem_hyperelastic_baseline_centerline.pdf}
    \caption{Final centerline $u_x$ profile on the common sample path.}
  \end{subfigure}
  \caption{Serial hyperelastic companion comparison against JAX-FEM on the
  same mesh and prescribed displacement schedule, with energy post-comparison
  of the terminal states under \cref{eq:hyperelasticity-energy}.}
  \label{fig:hyperelasticity-jaxfem-baseline}
\end{figure}

\begin{table}[H]
  \centering
  \input{tables/generated/jax_fem_hyperelastic_baseline.tex}
  \caption{Agreement-gated hyperelastic companion comparison against JAX-FEM.}
  \label{tab:hyperelasticity-jaxfem-baseline}
\end{table}

The agreement gate in \Cref{tab:hyperelasticity-jaxfem-baseline} passes on all
checked quantities. The final energy relative difference is
\num{8.244e-6}, the full-field displacement relative $L^2$ difference is
\num{2.011e-4}, the centerline relative $L^2$ difference is
\num{5.473e-5}, and the $u_{\max}$-curve relative $L^2$ difference is
effectively zero. On this small serial CPU companion case, the \jaxpetsc{} solve
is faster (\SI{0.176}{s} median wall time versus \SI{2.524}{s} for JAX-FEM),
but the scientific role of this table is solution agreement under a matched
hyperelastic definition rather than a software ranking.

\subsection{Plasticity3D Validation Ladder}
\label{sec:val-plasticity3d}

The \plasticitythree{} benchmark is tied to a Mohr--Coulomb slope-stability
source family, but the object studied here is the algorithmic
endpoint surrogate defined in
\Cref{sec:bench-plasticity3d,eq:plasticity3d-discrete,eq:plasticity3d-potential}.
The Sysala return-mapping, optimization, convex-optimization, and advanced
continuation papers, together with the efficient MATLAB elastoplastic
implementation of \citet{cermak2019efficient}, provide source-family and method
context for this problem class
\citep{sysala2017returnmapping,sysala2021optimization,sysala2025convexoptimization,sysala2025advancedcontinuation};
they are not used here as verified numeric paper baselines.

The validation ladder therefore separates two questions. Layer 1A tests
direct-branch source-observable agreement for the endpoint surrogate on the
$P_2(L_1)$ source-family case. This layer is tight: the work relative
difference is \num{3.877e-5}, the displacement relative $L^2$ difference is
\num{3.517e-3}, and the deviatoric-strain relative $L^2$ difference is
\num{8.720e-3}. Layer 2 is different: it is a fixed-$\lambda_{\mathrm{sr}}$
diagnostic
against a reference-operator comparison, not proof of path-consistent
incremental-history equivalence. After matching the source boundary conditions
to the glued-bottom free-DOF mask, the validation schedule is capped
at the main-figure load level $\lambda_{\mathrm{sr}}=1.55$. It keeps the
highest-successful-$\lambda_{\mathrm{sr}}$ schedule proxy aligned on the tested
grid and passes the predefined gates for the $u_{\max}(\lambda_{\mathrm{sr}})$
curve and endpoint displacement: the
$u_{\max}(\lambda_{\mathrm{sr}})$ relative $L^2$ difference is
\num{6.396e-6}, and the endpoint displacement relative $L^2$ difference is
\num{8.299e-4}. The endpoint deviatoric-strain relative $L^2$ difference
(\num{4.307e-3}) and boundary-profile relative $L^2$ difference
(\num{5.229e-4}) are reported as diagnostics rather than acceptance gates.
This comparison uses a matched mesh, material table, Davis-B strength
reduction, load schedule, and glued-bottom free-DOF mask, but it remains a
fixed-load endpoint check rather than a path-consistent plastic-history
reference.

\begin{figure}[H]
  \centering
  \begin{subfigure}[t]{\PaperFigureMedium}
    \centering
    \includegraphics[width=\linewidth]{plasticity3d_validation_layer1a_boundary.pdf}
    \caption{Layer 1A: direct-branch source-observable agreement.}
  \end{subfigure}

  \vspace{0.45em}

  \begin{subfigure}[t]{\PaperFigureMedium}
    \centering
    \includegraphics[width=\linewidth]{plasticity3d_validation_umax_curve.pdf}
    \caption{Layer 2: fixed-$\lambda_{\mathrm{sr}}$ reference-operator
    diagnostic.}
  \end{subfigure}
  \caption{Plasticity3D validation ladder. The upper panel documents tight
  direct-branch source-observable agreement for the endpoint surrogate; the
  lower panel shows close fixed-$\lambda_{\mathrm{sr}}$ reference-operator
  agreement after
  matching the glued-bottom boundary conditions and free-DOF mask.}
  \label{fig:plasticity3d-validation}
\end{figure}

\begin{table}[H]
  \centering
  \input{tables/generated/plasticity3d_validation_summary.tex}
  \caption{Plasticity3D validation summary for direct-branch source-observable
  agreement
  (layer 1A) and the fixed-$\lambda_{\mathrm{sr}}$ reference-operator
  diagnostic (layer 2).}
  \label{tab:plasticity3d-validation}
\end{table}

The resulting interpretation is limited but useful. \plasticitythree{} is
supported here by direct-branch source-observable agreement in Layer 1A, while
Layer 2 supports the same endpoint surrogate under a fixed-load
reference-operator comparison. Neither layer is a path-consistent
incremental-plasticity history validation.
