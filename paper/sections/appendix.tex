\label{sec:solver-evidence}

This appendix collects supporting solver-policy diagnostics that are
methodologically useful but narrower than the main benchmark narratives.

\subsection{Plasticity3D Reference-Formula and Fixed-Reference PMG Comparisons}

The primary Plasticity3D numerical story in \Cref{sec:num-plasticity3d} is the
rank-local constitutive-AD path. The tables below add two solver-policy
comparisons on the same $P_4(L_2)$ problem from
\cref{eq:plasticity3d-discrete} at $\lambda_{\mathrm{sr}}=\num{1.0}$: a
closed-form constitutive-tangent assembly comparator and a frozen-preconditioner
PMG variant.
Both use Armijo Newton, a gradient-only stopping target
$\|\nabla\mathcal{F}\|_2<\num{0.01}$, at most 50 Newton iterations, and PETSc
relative KSP tolerance \num{1e-1}. Here closed-form assembly means evaluating
the active constitutive branch and tangent formulas without AD, while
fixed-reference PMG means a frozen-preconditioner variant in which the multigrid
operator is not rebuilt along the nonlinear path.

\begin{table}[!htbp]
  \centering
  \input{tables/generated/plasticity3d_constitutive_vs_reference_formula.tex}
  \caption{Matched constitutive-assembly versus reference-formula assembly
  comparison for the same PMG solver configuration on
  $P_4(L_2)$ at $\lambda_{\mathrm{sr}}=\num{1.0}$. The table reports wall time,
  solve time, and their ratio for matched rank counts on
  \cref{eq:plasticity3d-discrete}.}
  \label{tab:plasticity3d-constitutive-reference-formula}
\end{table}

\begin{table}[!htbp]
  \centering
  \input{tables/generated/plasticity3d_fixed_reference_operator_pmg.tex}
  \caption{Fixed-reference PMG alternative on the same
  $P_4(L_2)$, $\lambda_{\mathrm{sr}}=\num{1.0}$ Plasticity3D problem. The status
  column distinguishes completed configurations from rank counts that reach the
  maximum number of Newton iterations before satisfying the target gradient
  tolerance.}
  \label{tab:plasticity3d-fixed-reference-pmg}
\end{table}

These diagnostics support two narrower statements. First, closed-form branch
tangent assembly has lower reported wall time than AD-branch tangent assembly in
the listed matched-rank comparisons, although the gap narrows at higher rank
counts.
Second, the fixed-reference PMG profile is not the default used in the
present study: it may appear favorable on completed low-rank configurations, but
three of the eight fixed-reference rows reach the maximum number of Newton
iterations and therefore give less consistent convergence evidence. The rank-local
constitutive-AD path remains the primary Plasticity3D realization because it uses the
same local differentiable formulation as the derivative-route and converged
scaling studies; the reference-formula configurations are retained to expose
solver-policy sensitivity on matched discrete problems.

\FloatBarrier

\subsection{Plasticity2D Reference Continuation Evidence}

For Plasticity2D, the reference-continuation comparison is not the primary
method of the paper, but it is still useful as fixed-policy rank and timing
evidence for a fixed same-mesh PMG policy under the same outer continuation
logic.

\begin{table}[H]
  \centering
  \input{tables/generated/plasticity2d_reference_continuation.tex}
  \caption{Reference continuation with the fixed PMG policy at 8 and 32 ranks.
  This is supporting solver evidence for a reference continuation
  setting, not a primary solver comparison.}
  \label{tab:plasticity2d-reference-continuation}
\end{table}

The two rows show fixed-policy timing evidence rather than a validation
baseline: increasing from 8 to 32 ranks reduces the reported wall time from
\SI{489}{s} to \SI{198}{s}, while the final strength-reduction values remain
close at \num{1.568403} and \num{1.568334}.
