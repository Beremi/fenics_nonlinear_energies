\label{sec:performance-solver-behavior}

Having defined the benchmark models and validation evidence, we now report
computational behavior: globalization, derivative-route cost, sparse-solver
policy, memory, and scaling. Timing rows from validation studies are used only
when they clarify solver behavior; validation and performance interpretations
remain separate. \Cref{tab:numerical-protocol-summary} fixes the row-level terms
used across the result blocks and follows the protocol in
\Cref{tab:solver-reporting-protocol}.

\begin{table}[!htbp]
  \centering
  \scriptsize
  \caption{Numerical evidence blocks and reporting protocol. Validation rows fix
  terminology only; performance claims use the stated timing, stopping, and
  solver-policy columns. The KSP column gives representative tolerance and
  iteration policies, overridden by result-specific rows.}
  \label{tab:numerical-protocol-summary}
  \input{tables/generated/numerical_protocol_summary.tex}
\end{table}

\subsection{Globalization Method Comparison}
\label{sec:num-globalization}

\Cref{tab:globalization-method-compare} asks whether one nonlinear
globalization policy is robust across the tested families. Each row holds the
element assembly path and preconditioner family fixed within the problem and
varies only the nonlinear policy. Timed-out rows are retained as capped solver
behavior rather than convergence evidence.

The rows are representative fixed cases rather than one physical problem:
$p$-Laplace uses a level-10 unit-load stress test, hyperelasticity the first
eight level-4 rank-local load steps, and Plasticity3D the $P_2(L_1)$ endpoint
surrogate at $\lambda_{\mathrm{sr}}=\num{1.55}$. The four rows use 32, 16, 32,
and 32 MPI ranks with 30, 120, 270, and 180 s caps.

\begin{table}[!htbp]
  \centering
  \footnotesize
  \caption{Fixed-case comparison of nonlinear globalization policies. LS denotes
  Armijo line search and TR denotes Steihaug trust region. The gradient-to-target
  column is reported for the Plasticity3D endpoint gate.}
  \label{tab:globalization-method-compare}
  \input{tables/generated/globalization_method_compare.tex}
\end{table}

The scalar and mechanics rows show different preferences. Newton with Armijo
line search is fastest on the smooth $p$-Laplace unit-load row, but the same
line-search-only policy times out on Ginzburg--Landau. For the hyperelasticity
excerpt all three policies complete; the hybrid policy reduces nonlinear
iterations and solver time relative to line-search-only Newton, while the pure
Steihaug variant has more trust-region rejections.

The Plasticity3D endpoint is the clearest case in this suite against a
universal policy. At the stricter $\|\nabla \mathcal{F}\|_2 < 10^{-4}$ criterion,
line-search-only Newton reaches the iteration cap, whereas both trust-region
policies converge. The capped row matches the displayed energy but remains above
the gradient target, as reported by the gradient-to-target column. The hybrid
variant is the faster trust-region configuration and also uses fewer Krylov
iterations. These comparisons support a problem-dependent policy choice rather
than a universal preference for any single globalization strategy.

We additionally evaluate the Ginzburg--Landau level-10 case under an eight-rank
budget. \Cref{tab:gl-globalization-fixed-budget} reports this scalar-nonconvex
comparison separately from the multi-family table above. Its purpose is
narrower: it records that the trust-region variants complete with matching
energy and Newton--Krylov work, while the line-search-only configuration still
times out under the fixed wall-time cap. For that timed-out configuration we
report elapsed wall time and the cap, whereas completed configurations report
solver time.

\begin{table}[!htbp]
  \centering
  \footnotesize
  \caption{Ginzburg--Landau level-10 fixed-budget globalization comparison on
  eight MPI ranks. The configurations use the same element assembly and Hypre
  preconditioner; only the nonlinear globalization policy changes. The timed-out
  configuration reports elapsed wall time and the stated wall-time cap.}
  \label{tab:gl-globalization-fixed-budget}
  \input{tables/generated/ginzburg_landau_globalization_fixed_budget.tex}
\end{table}

\subsection{Derivative-Route Comparison}
\label{sec:num-derivative-route}

\Cref{tab:derivative-route-compare} asks whether the second-order route changes
the terminal state or mainly changes the assembly and linearization cost. The
three matched cases compare element AD, colored sparse recovery, and, where
available, constitutive AD under the same nonlinear problem definition. The
timing-scope column states whether the reported time is a solver total, solve
time, or wall time for the specific configuration.

\begin{table}[!htbp]
  \centering
  \footnotesize
  \caption{Matched-case derivative-route comparison. Colored SFD denotes rank-local
  colored sparse recovery; the reported configurations use AD-HVP probes, while
  finite-difference gradient probes are the classical recovery variant. The
  SFD-color range is shown where per-rank color counts are available.}
  \label{tab:derivative-route-compare}
  \input{tables/generated/derivative_route_compare.tex}
\end{table}

Across all three benchmark cases, colored sparse recovery reaches the same
table-reported energy, up to displayed precision, and identical
nonlinear/Krylov iteration counts as element AD.
The scalar and hyperelasticity cases show a wall-time premium for colored sparse
recovery. On the Plasticity3D endpoint, constitutive AD is fastest, while
element AD and colored sparse recovery have matching iteration counts and
comparable solve times. The colored sparse-recovery route requires 240--420
colors across the 32 ranks; its comparable solve time is consistent with batched
rank-local HVPs having nearly the same measured Hessian-evaluation cost as
element AD on this $P_2(L_1)$ case.

\Cref{tab:plasticity3d-derivative-degree} measures Plasticity3D
Hessian-evaluation cost for one Newton linearization, not nonlinear convergence.
The key same-DOF contrast is $P_1(L_2)$ versus $P_2(L_1)$: at \num{79024} free
unknowns, the denser $P_2$ stencil raises colors from 66--90 to 240--420,
Hessian time from \num{0.332} to \num{1.412} s, and rank-maximum resident set
size (RSS) from about \num{0.65} to \num{1.10} GiB. The $P_4$ colored
sparse-recovery case is outside this table.

\begin{table}[!htbp]
  \centering
  \footnotesize
  \caption{Plasticity3D one-Newton derivative-route cost versus discretization.
  Elem DOFs is the local vector element size used by the Hessian evaluation;
  overlap DOFs is the maximum local overlap vector size recorded during
  assembly.}
  \label{tab:plasticity3d-derivative-degree}
  \input{tables/generated/plasticity3d_derivative_degree.tex}
\end{table}

\FloatBarrier

\subsection{\texorpdfstring{$p$-Laplace}{p-Laplace}}
\label{sec:num-plaplace}

The $p$-Laplace problem definition is given in \Cref{sec:bench-plaplace}, with
discrete energy \eqref{eq:plaplace-discrete}. In this scalar setting, the
numerical evidence is clearest: exact element Hessians remain
effective in the reported distributed configurations, while colored SFD reaches nearly
identical discrete energies at higher wall time.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\PaperFigureMedium]{plaplace_scaling.pdf}
  \caption{$p$-Laplace strong scaling on the finest tested grid. All curves
  correspond to the same discrete problem from \eqref{eq:plaplace-discrete}.}
  \label{fig:plaplace-results}
\end{figure}

In the finest plotted case, the \jaxpetsc{} element-AD path reports
\SI{1.14}{s} on 32 ranks, while colored SFD reports \SI{1.45}{s} with the same
Newton and Krylov iteration counts. For the matched 32-rank case summarized in
\Cref{tab:derivative-route-compare}, the reported energies differ by about
$\num{8e-9}$, so this block mainly measures the overhead of sparse recovery on a
smooth scalar problem. Across the plotted ranks, element AD drops from
\SI{10.13}{s} on one rank to \SI{1.14}{s} on 32 ranks; colored SFD follows the
same trend with persistent recovery overhead. The FEniCS curve in the same
figure provides reference-implementation context; the comparison above isolates
the two \jaxpetsc{} derivative routes.

\subsection{Ginzburg--Landau}
\label{sec:num-gl}

The Ginzburg--Landau family from \Cref{sec:bench-gl}, with discrete problem
\eqref{eq:gl-discrete}, shows the same high-level derivative story under a more
indefinite linearization. The computational point is not a dramatic separation
between element-Hessian and colored sparse-recovery routes, but that the same
sparse infrastructure handles the reported nonconvex scalar-energy
configurations.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\PaperFigureMedium]{ginzburg_landau_scaling.pdf}
  \caption{Ginzburg--Landau strong scaling for the tested fine-grid case
  governed by \eqref{eq:gl-discrete}.}
  \label{fig:gl-results}
\end{figure}

\FloatBarrier

At 32 ranks, the element-AD and colored sparse-recovery \jaxpetsc{} configurations report
\SI{2.10}{s} and \SI{2.72}{s}, respectively. The scaling figure shows the same
final-energy trend for the two routes, so the main visible difference is the
colored sparse-recovery wall-time premium on this nonconvex scalar energy.
Across the plotted ranks, element AD falls from \SI{15.64}{s} on one rank to
\SI{2.10}{s} on 32 ranks, with a nonideal plateau around 4--8 ranks before the
32-rank point. The FEniCS curve is included as reference-implementation context,
while the paragraph above isolates the \jaxpetsc{} derivative-route cost.

\subsection{Hyperelasticity}
\label{sec:num-hyperelasticity}

\Cref{sec:bench-he} defines the boundary data and
\eqref{eq:hyperelasticity-discrete}. Here the full load path tests
globalization and preconditioning; derivative-route evidence is limited to the
first-step replicated-element case. The distributed implementation completes the
load path, \Cref{fig:hyperelasticity-results} reports only its scaling, and the
JAX-FEM comparison remains in \Cref{sec:val-hyperelasticity}.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\PaperFigureMedium]{hyperelasticity_scaling.pdf}
  \caption{Hyperelasticity strong scaling for the 24-step load path defined by
  \eqref{eq:hyperelasticity-discrete}.}
  \label{fig:hyperelasticity-results}
\end{figure}

\FloatBarrier

The 24-step hyperelasticity scaling curve reduces \jaxpetsc{} element-AD wall
time from \SI{1894}{s} at 8 ranks to \SI{529}{s} at 32 ranks. The speedup is
substantial but not ideal, which motivates the more targeted fixed-work
distribution and PMG diagnostics below.

The fixed-work multi-node experiment asks how rank-local level-5 first-step
assembly behaves under common nonlinear work while rank counts and PMG
coarse-solver layouts vary. CPU configurations use one MPI rank per core on
dual-socket AMD EPYC 7H12 nodes with 256 GB RAM
\citep{it4i-karolina-compute-nodes}, contiguous CPU binding, and
memory binding local to each non-uniform-memory-access domain. The uniform-beam
hierarchy uses rank-local elements, PETSc coordinate-format preallocation, and
point-to-point overlap exchange.

The nonlinear solve is the same trust-region Newton path used for the
benchmark in \cref{sec:bench-he}. The linear subproblems use
Steihaug--Toint conjugate-gradient iteration with a same-hierarchy PMG
preconditioner, Chebyshev/Jacobi smoothing, and coarsest level $L_2$. The coarse
solve is PETSc redundant LU with MUMPS: the partial one-node configurations use
one redundant group over the active ranks, while the
multi-node configurations use one 128-rank redundant group per node.
\Cref{fig:hyperelasticity-cpu-pmg-scaling,tab:hyperelasticity-cpu-pmg-scaling}
report measured solver total time rather than scheduler elapsed time.
All configurations reach the same nonlinear and linear iteration counts shown in
\Cref{tab:hyperelasticity-cpu-pmg-scaling}: 21 Newton iterations and
43 Krylov iterations.

\Cref{tab:hyperelasticity-distribution-memory} reports the memory evidence that
supports using the rank-local assembly variant in the larger CPU sweeps. The
fixed-work agreement rows compare the replicated and rank-local level-4
first-step solve at four MPI ranks; the one-linearization memory rows use one
Newton linearization on the level-5 mesh at 8, 16, and 32 ranks. The table reports
energy, operating-system peak RSS, and the measured footprint of assembly
arrays, making clear whether a configuration stores global mesh and adjacency
data or only local elements plus overlap.

\begin{table}[!htbp]
  \centering
  \scriptsize
  \caption{Rank-local hyperelasticity memory and overlap evidence. RSS max/sum
  gives the maximum and summed per-rank resident set size. Tracked sum is the
  reported assembly-array footprint, and overlap/owned is the summed rank-local
  overlap DOF count divided by global owned free DOFs for the rank-local overlap
  assembly.}
  \label{tab:hyperelasticity-distribution-memory}
  \input{tables/generated/hyperelasticity_distribution_memory.tex}
\end{table}

The level-4 fixed-work rows show matching Newton and Krylov counts and the same
rounded energy value for replicated and rank-local assembly. On the larger
level-5 one-linearization rows, rank-local assembly reduces summed RSS
from \SI{215.6}{GiB} to \SI{104.9}{GiB} at 32 ranks, with solve times
\SI{3.37}{s} and \SI{3.36}{s}. The level-5 energy values are fixed-work
terminal states after one linearization, so these rows are memory/cost
diagnostics rather than endpoint-agreement checks.

\Cref{tab:hyperelasticity-pmg-sensitivity} isolates the first-step level-5 PMG
choice on the same 32-rank level-5 problem with fixed nonlinear work. All
configurations perform the same eight Newton linearizations, so the table
compares the cost and terminal state reached under different preconditioner and
coarse-solver policies. The energy column is the state after this fixed
nonlinear work, so
\Cref{tab:hyperelasticity-pmg-sensitivity} is a solver-policy comparison rather
than an equal-accuracy timing comparison or endpoint-accuracy ranking.

\begin{table}[!htbp]
  \centering
  \footnotesize
  \caption{Rank-local hyperelasticity level-5 PMG and coarse-solver sensitivity at
  32 MPI ranks with fixed nonlinear work. The PMG configurations use the same
  rank-local element path as the scaling configurations.}
  \label{tab:hyperelasticity-pmg-sensitivity}
  \input{tables/generated/hyperelasticity_pmg_sensitivity.tex}
\end{table}

\FloatBarrier

The fixed-work table reports that the MUMPS-coarse PMG variants reduce total
Krylov iterations from 92 with Hypre coarse solves to 17, and reduce solver time
from \SI{23.3}{s} to about \SI{16}{s}. This is preconditioner-policy evidence
for a fixed nonlinear workload, not a statement about final load-path accuracy.
The energy differences in the table are fixed-work terminal-state differences
after the same eight Newton linearizations.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\PaperFigureMedium]{hyperelasticity_cpu_pmg_scaling.pdf}
  \caption{Single- and multi-node CPU level-5 hyperelasticity first-step scaling
  at fixed nonlinear work with the PMG coarse level fixed at $L_2$. Annotations
  show CPU-node counts.}
  \label{fig:hyperelasticity-cpu-pmg-scaling}
\end{figure}

\FloatBarrier

\begin{table}[!htbp]
  \centering
  \caption{Single- and multi-node CPU level-5 hyperelasticity first-step timings used in
  \cref{fig:hyperelasticity-cpu-pmg-scaling}. This is a fixed-work first-step
  timing study with a common PMG hierarchy, not a full load-path comparison.}
  \label{tab:hyperelasticity-cpu-pmg-scaling}
  \input{tables/generated/hyperelasticity_cpu_pmg_scaling.tex}
\end{table}

\FloatBarrier

\Cref{fig:hyperelasticity-cpu-pmg-scaling,tab:hyperelasticity-cpu-pmg-scaling}
show a fixed-work timing drop from \SI{419}{s} on 16 ranks to \SI{15.7}{s} on
1024 ranks, with the same 21 Newton iterations and 43 Krylov iterations in all
listed configurations. This evidence isolates the multi-node sparse-solver and
PMG-coarse-level behavior for one load step, rather than the accuracy of the
full load path.

\subsection{Plasticity2D}
\label{sec:num-plasticity2d}

The Plasticity2D model is defined in \Cref{sec:bench-plasticity2d}, with
Davis-B reduction \eqref{eq:plasticity2d-davis} and discrete surrogate
\eqref{eq:plasticity2d-discrete}.
For this family, the most informative computational evidence in the present
paper is not a single strong-scaling curve. The benchmark section separates the
completed endpoint case from the larger fixed-cost studies in
\Cref{fig:plasticity2d-energy-levels,tab:plasticity2d-benchmark}, and the
reference-continuation comparison is collected later in
\Cref{sec:solver-evidence}.

The $P_4(L_5)$ case is the endpoint evidence for this family. The completed
endpoint has energy \num{-212.538} and wall time \SI{147}{s}. The larger
$P_4(L_6)$ and $P_4(L_7)$ cases are fixed-work diagnostics for nonlinear-tail
and PMG behavior; both report energy \num{-212.539} after 20-iteration caps,
with wall times \SI{147}{s} and \SI{253}{s}.

The 8-rank and 32-rank reference-continuation rows in
\Cref{tab:plasticity2d-reference-continuation} support solver-policy timing
under a common continuation logic. They are not a separate validation result or
a strong-scaling claim. In this diagnostic, increasing from 8 to 32 ranks
reduces wall time from \SI{489}{s} to \SI{198}{s}, while the continuation
Krylov totals differ, 733 versus 829.

\subsection{Plasticity3D}
\label{sec:num-plasticity3d}

The governing problem is defined in \Cref{sec:bench-plasticity3d}, with
Davis-B reduction \eqref{eq:plasticity3d-davis}, scalar constitutive potential
\eqref{eq:plasticity3d-potential}, discrete surrogate
\eqref{eq:plasticity3d-discrete}, and deviatoric-strain norm
\eqref{eq:plasticity3d-devnorm}.
The validation limits for this endpoint surrogate are reported in
\Cref{sec:val-plasticity3d}. With that interpretation fixed, this section asks
two performance questions: which second-order route is fastest on the fixed
high-order derivative-route case under matched terminal observables, and how
the endpoint surrogate behaves across degree, resolution, and rank count.

\paragraph{Derivative-route comparison.}
The first question is whether the second-order route changes the terminal
observables or only the cost of derivative construction. On the fixed
$P_4(L_1)$ case at $\lambda_{\mathrm{sr}}=\num{1.5}$ and 8 MPI ranks,
\Cref{fig:plasticity3d-derivative-ablation,tab:plasticity3d-derivative-ablation}
show the same Newton counts, total Krylov counts, and table-reported
terminal observables to displayed precision across the three routes. In the
table, $\omega$ denotes the external-work
observable and $u_{\max}$ denotes the maximum displacement magnitude.

The differences are cost differences. Constitutive AD has the lowest reported
wall time on this fixed case, with wall time \SI{222}{s} and solve time
\SI{191}{s}, compared with \SI{288}{s}/\SI{255}{s} for element AD and
\SI{372}{s}/\SI{253}{s} for colored SFD. This residual-bisection Newton
comparison uses the Hypre-backed rank-local linear-solver profile; it is
therefore separate from the MUMPS-backed PMG configurations used by the later
$\lambda_{\mathrm{sr}}=\num{1.55}$ convergence studies.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\PaperFigureMedium]{plasticity3d_derivative_ablation_bars.pdf}
  \caption{Fixed Plasticity3D derivative-route comparison on
  $P_4(L_1)$, $\lambda_{\mathrm{sr}}=\num{1.5}$, and 8 MPI ranks.}
  \label{fig:plasticity3d-derivative-ablation}
\end{figure}

\begin{table}[!htbp]
  \centering
  \caption{Plasticity3D derivative-route comparison on the fixed
  $P_4(L_1)$, $\lambda_{\mathrm{sr}}=\num{1.5}$, 8-rank case. These configurations use the
  Hypre-backed rank-local profile and are separate from the MUMPS-backed PMG
  configurations in the $\lambda_{\mathrm{sr}}=\num{1.55}$ studies.}
  \label{tab:plasticity3d-derivative-ablation}
  \input{tables/generated/plasticity3d_derivative_ablation.tex}
\end{table}

\FloatBarrier

\paragraph{Discretization and scaling.}
The remaining figures in this subsection answer a different question: how the
endpoint surrogate behaves across degree/resolution choices, and how the
selected solver configurations scale on the separate $P_4(L_2)$ timing studies.
The zoomed panels in \Cref{fig:plasticity3d-degree-energy} are included because
the full figure is dominated by coarse $P_1$ variation. Under the bottom-clamped
constraint, the matched set at \num{610964} free DOFs is
$P_1(L_3)$, $P_2(L_2)$, and $P_4(L_1)$. The matched set at
\num{4801816} free DOFs is $P_1(L_4)$, $P_2(L_3)$, and $P_4(L_2)$. The
scientific message from
\Cref{fig:plasticity3d-degree-energy,tab:plasticity3d-benchmark} is therefore
that the solver reaches similar terminal energies along both $p$- and
$h$-refined paths, while higher order on coarser meshes buys lower observed
values of \eqref{eq:plasticity3d-discrete} at matched DOFs only by paying a
clear wall-time premium in this nine-case endpoint study.
At \num{610964} DOFs, for example, $P_4(L_1)$ reports energy \num{-3013227}
and wall time \SI{208}{s}, compared with \num{-3013032} and \SI{98.3}{s} for
$P_2(L_2)$. At \num{4801816} DOFs, $P_4(L_2)$ reports \num{-3013348} and
\SI{2298}{s}, compared with \num{-3013149} and \SI{1330}{s} for $P_2(L_3)$.

\begin{figure}[!htbp]
  \centering
  \begin{subfigure}[t]{0.46\PaperFigureFull}
    \centering
    \includegraphics[width=\linewidth]{plasticity3d_degree_energy_all_dofs.pdf}
    \caption{All degrees: final energy versus free DOFs.}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.46\PaperFigureFull}
    \centering
    \includegraphics[width=\linewidth]{plasticity3d_degree_energy_all_time.pdf}
    \caption{All degrees: final energy versus total wall time.}
  \end{subfigure}

  \vspace{0.45em}

  \begin{subfigure}[t]{0.46\PaperFigureFull}
    \centering
    \includegraphics[width=\linewidth]{plasticity3d_degree_energy_zoom_dofs.pdf}
    \caption{$P_2/P_4$ zoom: final energy versus free DOFs.}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.46\PaperFigureFull}
    \centering
    \includegraphics[width=\linewidth]{plasticity3d_degree_energy_zoom_time.pdf}
    \caption{$P_2/P_4$ zoom: final energy versus total wall time.}
  \end{subfigure}
  \caption{Bottom-clamped Plasticity3D
  degree-versus-resolution study at $\lambda_{\mathrm{sr}}=\num{1.55}$. The top
  panels show the full $P_1/P_2/P_4$ comparison, while the bottom panels zoom to
  $P_2/P_4$ so the near-overlap hidden by the coarse $P_1$ points becomes
  visible. The left column plots final values of
  \eqref{eq:plasticity3d-discrete} against free DOFs; the right column plots
  the same converged energies against end-to-end wall time. All timings use the
  same 32-rank configuration.}
  \label{fig:plasticity3d-degree-energy}
\end{figure}

\FloatBarrier

\begin{table}[!htbp]
  \centering
  \caption{Separate Plasticity3D strong-scaling study for \localpmg{} on
  $P_4(L_2)$ at $\lambda_{\mathrm{sr}}=\num{1.0}$. These timings provide
  solver evidence for a separate load factor from the bottom-clamped
  $\lambda_{\mathrm{sr}}=\num{1.55}$ benchmark figures.}
  \label{tab:plasticity3d-scaling}
  \input{tables/generated/plasticity3d_recommended_scaling.tex}
\end{table}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\PaperFigureMedium]{plasticity3d_recommended_scaling.pdf}
  \caption{Separate strong-scaling study for the selected Plasticity3D solver
  configuration: \localpmg{} on $P_4(L_2)$ at
  $\lambda_{\mathrm{sr}}=\num{1.0}$. This is a separate timing study from the
  bottom-clamped $\lambda_{\mathrm{sr}}=\num{1.55}$ results shown in
  \cref{fig:plasticity3d-state,fig:plasticity3d-convergence,fig:plasticity3d-degree-energy}.}
  \label{fig:plasticity3d-scaling}
\end{figure}

\FloatBarrier

\Cref{fig:plasticity3d-scaling,tab:plasticity3d-scaling} give a high-DOF
Plasticity3D distributed timing result for the Hypre-coarse PMG variant. This
scaling evidence uses the selected $P_4(L_2)$ mesh family but a
different load factor, $\lambda_{\mathrm{sr}}=\num{1.0}$, so it complements rather
than extends the bottom-clamped $\lambda_{\mathrm{sr}}=\num{1.55}$ benchmark figures
above. Within that timing study, the reported nonlinear and linear iteration
counts remain matched on 1/2/4/8/16/32 ranks, while the end-to-end time drops
from \SI{5424}{s} on one rank to \SI{300}{s} on 32 ranks. Additional
reference-formula assembly and continuation evidence is reported later in
\Cref{sec:solver-evidence}.

The converged bottom-clamped $P_4(L_2)$,
$\lambda_{\mathrm{sr}}=\num{1.55}$ scaling comparison fixes nonlinear work: all
configurations converge in 29 Newton iterations and 1240 Krylov iterations to
the same displayed energy and stopping-gradient norm. It uses constitutive-AD
rank-local assembly, MUMPS-backed $P_4 \rightarrow P_2 \rightarrow P_1$ PMG,
Armijo Newton, KSP tolerance \num{1e-2}, and a stop requiring relative
correction below \num{0.002} and stopping-gradient norm below one percent of the
first Newton gradient. Since all reported
$\|g\|_{\mathrm{stop}}/\|g\|_{\mathrm{target}}<1$,
\Cref{fig:plasticity3d-cpu-scaling,tab:plasticity3d-cpu-scaling} compare CPU
timings at fixed nonlinear workload.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\PaperFigureMedium]{plasticity3d_cpu_scaling.pdf}
  \caption{Single-node and multi-node CPU converged scaling for the bottom-clamped
  Plasticity3D $P_4(L_2)$ case at $\lambda_{\mathrm{sr}}=\num{1.55}$. Dashed
  references are ideal $1/r$ lines anchored separately to the first point of
  each CPU setting.}
  \label{fig:plasticity3d-cpu-scaling}
\end{figure}

\begin{table}[!htbp]
  \centering
  \scriptsize
  \caption{Converged timings for the Plasticity3D
  $P_4(L_2)$, $\lambda_{\mathrm{sr}}=\num{1.55}$ configuration shown in
  \cref{fig:plasticity3d-cpu-scaling}. Single-node configurations use one CPU
  node; multi-node configurations use 16 ranks per CPU node. The nonlinear
  stop requires relative correction below \num{0.002} and final gradient below
  one percent of the first Newton gradient. The stopping-gradient norm in this
  table is the relative-gradient stopping metric for this scaling comparison, not the
  absolute final-gradient column in the degree benchmark table.}
  \label{tab:plasticity3d-cpu-scaling}
  \input{tables/generated/plasticity3d_cpu_scaling.tex}
\end{table}

\FloatBarrier

The distributed layout measurements in
\Cref{tab:plasticity3d-cpu-partitioning} identify one observed limit
on ideal scaling. The maximum local overlap vector shrinks with rank count,
but the sum of rank-local overlap DOFs increases from about the owned problem
size on one rank to more than four times the owned size on 128 ranks. This
overlap replication comes from subdomain boundaries and high-order element
support, so it is a decomposition cost rather than duplicated global mesh
storage.

\begin{table}[!htbp]
  \centering
  \caption{Distributed layout measurements for the converged Plasticity3D
  scaling comparison. ``Max local DOFs'' is the largest local-overlap vector size on
  any rank; ``Overlap / owned DOFs'' is the summed rank-local overlap DOF count
  divided by the global owned free-DOF count.}
  \label{tab:plasticity3d-cpu-partitioning}
  \input{tables/generated/plasticity3d_cpu_partitioning.tex}
\end{table}

\FloatBarrier

\subsection{Topology Optimization}
\label{sec:num-topology}

The reduced design problem and objective history are defined in
\Cref{sec:bench-topology}, with mechanics subproblem \eqref{eq:topology-mechanics}
and reduced objective \eqref{eq:topology-reduced-objective}.
The numerical question here is distributed outer-loop stability: the mechanics
solve and the design update must remain synchronized under continuation in $p$
and move-regularized updates. Because the outer-loop work can vary with the
continuation schedule, we report end-to-end timing under adaptive stopping
rather than strong scaling at a fixed computational workload.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\PaperFigureMedium]{topology_scaling.pdf}
  \caption{Topology end-to-end parallel timing trend for the
  fine-grid configuration. The plotted wall times correspond to adaptive
  end-to-end solves of the coupled reduced-design problem defined by
  \eqref{eq:topology-reduced-objective}, with
  per-rank stopping behavior reported in \cref{tab:topology-summary}.}
  \label{fig:topology-results}
\end{figure}

\FloatBarrier

\begin{table}[!htbp]
  \centering
  \caption{Parallel topology endpoint observables and timing for the fine-grid
  reduced-design study. Wall time is adaptive end-to-end timing, not fixed-work
  strong scaling.}
  \label{tab:topology-summary}
  \input{tables/generated/topology_summary.tex}
\end{table}

\Cref{fig:topology-results,tab:topology-summary} report fine-grid wall time
decreasing from \SI{208}{s} on one rank to \SI{25.1}{s} on 32 ranks. Outer
iterations, final continuation exponents, compliance
(\num{8.9473}--\num{9.9074}), and achieved volume fractions
(\num{0.3749}--\num{0.3932}) vary across ranks. Thus this is adaptive
end-to-end behavior under the multiplier and stopping rule, not target-volume
equality, fixed-work strong scaling, or a rank-invariant endpoint.

\Cref{tab:topology-rank-consistency} complements the end-to-end timing
table with a controlled 40-iteration rank-consistency test on a smaller
$384\times192$ grid. Early convergence and stall stopping are disabled, and all
rank counts use the same initial design and smoother continuation schedule
($p \leftarrow p+\num{0.1}$). The final density field is compared with the
one-rank \jaxpetsc{} reference run after 40 outer iterations.
All rank counts complete the same 40 outer iterations. The compliance
difference stays below \num{0.2}\%, and the density relative $L^2$ difference
remains below \num{0.04}. These are the largest observed discrepancies for this
controlled 40-iteration case, not general rank-invariance thresholds.

\begin{table}[!htbp]
  \centering
  \footnotesize
  \caption{Topology controlled 40-iteration rank-consistency check. Each rank count completes the
  same 40 outer iterations; compliance and density differences are relative to
  the one-rank case.}
  \label{tab:topology-rank-consistency}
  \input{tables/generated/topology_rank_consistency.tex}
\end{table}

\FloatBarrier

The topology case extends the evidence from state solves to coupled
design--mechanics optimization. Here the primary distributed \jaxpetsc{}
realization is less about exact Hessians than about stable distributed
design-gradient exchange and mechanics coupling, while \purejax{} remains the
compact serial design demonstration discussed earlier in
\Cref{tab:reference-availability}.

\subsection{Synthesis of the Computational Evidence}
\label{sec:num-synthesis}

The results above provide the evidence blocks used in \Cref{sec:discussion}.
The globalization tests separate problem-dependent nonlinear policies; the
derivative-route comparisons isolate cost under fixed terminal observables; the
distribution, PMG, and partitioning studies expose solver-policy and
sparse-ownership effects; and the topology study closes with an end-to-end
design-and-mechanics loop plus a controlled rank-consistency check. Read
together, these measurements support the paper's main message: local
differentiable constitutive or energy evaluations can be coupled to PETSc-owned
sparse nonlinear and linear solves across scalar PDEs, mechanics, plasticity,
and topology, with comparator evidence scoped by problem family.
